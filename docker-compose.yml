
services:
  mongodb:
    image: mongo:7
    container_name: go-study-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: gostudy
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7
    container_name: go-study-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  mongo_data:
  redis_data: